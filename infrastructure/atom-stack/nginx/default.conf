server {
  listen 8080;
  server_name _;

  location = /healthz {
    add_header Content-Type application/json;
    return 200 '{"service":"atom-host","status":"healthy"}';
  }

  location / {
    add_header Content-Type text/plain;
    return 200 'AtoM host stack container is running';
  }
}
